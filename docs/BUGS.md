# Реестр багов и ошибок

> Этот файл ведётся агентом автоматически. Когда пользователь сообщает об ошибке —
> она добавляется сюда, чтобы не теряться. Статусы: `открыт`, `в работе`, `исправлен`, `отложен`.
> Версия — в которой проблема замечена/исправлена.

| # | Дата | Статус | Проблема | Версия | Заметки |
|---|------|--------|----------|--------|---------|
| 1 | 2026-08-07 | исправлен | Лупы DeepSeek: модель повторяет одинаковые tool-вызовы десятки раз (vk_fp8_probe ×75, один и тот же тест ×25). Детектор `detectRepeatedToolCallLoop` никогда не срабатывал — получал `inspectionMessages` в режиме `toolResultMode:"user"`, где все `tool_calls` вырезались. | 1.10.63 | Починено: отдельная конверсия `loopInspectionMessages` (tool-режим); кросс-ходовый nudge по окну 8 ходов; жёсткий лимит `maxModelTurnsPerRequest` (6). Логи: `chat.tools.loop_detected`, `chat.response.tool_call_only_nudge_cross_turn`, `chat.response.max_turns_stop`. |
| 2 | 2026-08-07 | исправлен | Мысли DeepSeek иногда уходят как ответ (видимый текст) и раздувают окно чата. Все ходы: thinkingMode=deep, preserve=false. Мысли идут как `reasoning_content` → сворачиваются; когда сервер кладёт рассуждения в `delta.content` — попадают в видимый текст (парсер резал только `think>`-теги). | 1.10.65 | Фикс в src/base-provider.ts: 1) `extractThinkingFromVisibleText` переписан на мульти-маркеры: `think>`, `|thinking|>`, `thinking>` (статические THINKING_MARKERS); 2) `coalescer.flush()`: фолбэк thinking без `LanguageModelThinkingPart` больше НЕ отдаёт мысли как видимый `TextPart` (скрывает, симметрично `emitThinkingText`); 3) буфер частичного маркера в конце потока эмитится текстом только вне секции рассуждений. 311 passing. Если мысли снова появятся как текст — включить `llamacpp.logStreamChunks: true` и прислать фрагмент. |
| 3 | 2026-08-07 | исправлен | Слишком жёсткая компакция: история 187 → 15 сообщений (36.5K токенов вместо ожидаемых ~60% ≈ 108K). autoCompacted=true, cache hit упал 98.9% → 18.2%. Запрос 0be8cd17, ход 75, conversationKey 5020979415fcdcaf. | 1.10.64 | **Причина:** токены распределены неравномерно — ранние ходы с огромными tool-результатами (до 22K chars) не помещались в бюджет 117K целиком, бинарный поиск по ходам оставлял только лёгкий хвост (15 сообщений), бюджет пустовал на 75%. **Фикс:** новые `llamacpp.compactMaxToolResultChars` (default 8000, 1000–24000): при компакции длинные tool-результаты в сохраняемом хвосте усекаются, поэтому в бюджет влезает в разы больше ходов (опция `maxToolResultChars` в `compactMessages`). Добавлен регрессионный тест «skewed token distribution» (311 passing). |
| 4 | 2026-08-07 | исправлен | Агент (chat-assistant) завис/подморозился посреди задачи — перестал отвечать, задача (создание файла трекинга багов) не была выполнена в срок. | 1.10.93 | Симптом: длинная пауза без ответа, пользователь вынужден напоминать. 2026-08-08: подтверждено пользователем, что агент больше не виснет; в живых логах 1.10.93 нет ни одного хода с потерей ответа (31/31 turn.complete, 0 застрявших запросов). Первопричина не была в коде расширения — фиксировалось поведение модели/хоста; закрыто как невоспроизводимое на текущей версии. |
| 5 | 2026-08-07 | исправлен | Агент не создал файл трекинга багов по первой просьбе (связано с #4) — процессное нарушение. | — | Файл `docs/BUGS.md` создан. Правило: при первой просьбе о трекинге создавать файл сразу, до начала других работ. |
| 7 | 2026-08-07 | исправлен | Бесконечный луп tool-вызовов в чате 18a9febd: 413 ходов turn.complete за ~3 часа (04:28–07:53 UTC), модель «сидела в лупе» часами. | 1.10.63 | **Причина (из логов):** ходы шли на **1.10.52/1.10.57** — ДО всех фиксов лупов. Детектор повторов не работал (баг #1: tool_calls вырезались конверсией user-режима), maxModelTurnsPerRequest и кросс-ходовый nudge не существовали; loop_detected/max_turns_stop/nudge_cross_turn в логах: 0. **Фикс:** все три защиты появились в 1.10.63 (детектор на tool-сообщениях, лимит 6 раундов/запрос, nudge при ≥4 tool-only ходах из 8) — в 1.10.67 на месте. Проверить при следующем лупе: в логах должны появиться loop_detected/max_turns_stop. |
| 9 | 2026-08-07 | исправлен | Первый ход после рестарта: история «раздувается» (324 → 778 сообщений) и полный кэш-мисс (26%). Ход fac2a61d, конв. 5020979415fcdcaf. | 1.10.69 | **Причина:** VS Code после рестарта пересобирает историю (вложения/контент становятся полными); findNewMessagesAfterSnapshot искал одиночный пивот (последнее сообщение снапшота) — при изменении хвоста пивот не находился → снапшот отбрасывался (pivot_not_found), отправлялся полный источник VS Code (778 вместо 324) → кэш-мисс. **Фикс:** пивот по K-грамме — ищем 3 последних сообщения снапшота подряд в источнике (SNAPSHOT_PIVOT_GRAM=3): устойчиво к пересборке 1-2 сообщений хвоста. Плюс в 1.10.69: deepSeekDefaultMaxOutputTokens 131072 → 70000 (reasoning входит в output-токены; ограничивает «бесконечные мысли»/лупы в одном ходе). 311 passing. |
| 8 | 2026-08-07 | исправлен | «Хаос» размера контекста в Live Report: 122K → 178K (+50K, полный кэш-мисс 4.5%) → 167K → 141K → 174K без единой компакции (autoCompacted: false во всех ходах 1–6). | 1.10.68 | **Причина (лог 12:43–12:47, конв. 5020979415fcdcaf):** 1) VS Code переписывает уже отправленные сообщения (history_rewritten «message 2 of 268») — префикс кэша расходится → кэш-мисс и полный пересчёт; 2) снапшот-логика: при переписывании истории пивот findNewMessagesAfterSnapshot находится раньше, и хвост дублируется (messageCount 327 при source 270, 334 при 268) — контекст раздувается; 3) эвристика токенов расходится с реальными prompt_tokens DeepSeek на ±25% (эвристика 156K, сервер 178K). **Фикс:** в prepareMessagesForBudget добавлен детектор дублей — если effectiveIfChained > sourceMessages.length+2 (снапшот+хвост длиннее полного источника), снапшот помечается stale (log chat.messages.snapshot_stale reason=rewritten_history_would_duplicate) и используется полный источник — без дублей, префикс стабилен. 311 passing. |
| 6 | 2026-08-07 | исправлен | Качество контекста моделей ПОСЛЕ компакции: агенты стали хуже работать; компакция съедала ~40-50% контекста (ход 9bef586f: 218K→129K, 426→185 сообщений; снапшот 85c07299: 201K→113K при target=0.6×). | 1.10.67 | **Диагностика:** снапшоты компакций работают (`<globalStorage>/compactions/`); проверка показала — качество summary/хвоста нормальное (хвост актуальный, summary с деталями кода/путей), проблема была в коэффициенте сжатия: компакция делала ровно target=0.6×текущего размера. **Фикс:** `COMPACTION_TARGET_RATIO` 0.6 → **0.75** (src/context/context-budget.ts) — потеря ~25% вместо ~40%, после компакции ~58% usage вместо 50%. Тесты обновлены (311 passing). Контроль после установки: новый снапшот должен показывать before→after с сохранением ~75%. |
| 10 | 2026-08-07 | исправлен | Внезапные кэш-миссы 1.8–2.3% посреди чата: `ident=3 msgs=875/873` (префикс расходился на индексе 1-3). | 1.10.70 | **Причина:** VS Code перегенерирует `<workspace_info>` (дерево файлов) в ПЕРВОМ user-сообщении на каждый запрос — любой новый файл в корне (snapshot-*.html, live.html, nul, tsc) переписывал msg #1. Плюс `stabilizeMessagePrefix` со стратегией «bytes» переиспользовала только байтово совпавшие сообщения (3 из 875), а ветка «aggressive» теряла свежий user-ход. **Фикс:** позиционное выравнивание — сообщение заменяется ранее отправленной версией при совпадении байтов, ИЛИ tool_call_id / id всех tool_calls, ИЛИ текста после маскировки волатильных блоков (`normalizeVolatileHostContext`: environment_info, workspace_info, context, attachments, reminderInstructions). Последнее сообщение никогда не заменяется. |
| 11 | 2026-08-07 | исправлен | Первый ход после рестарта расширения: полный miss (1%), классификация `system_prompt_changed` (вводила в заблуждение — винила VS Code, виноваты мы). Ход 66ac3619, 387 msg вместо 316. | 1.10.71 | **Причина:** `findNewMessagesAfterSnapshot` искал пивот по 3 последним сообщениям снапшота с БАЙТОВЫМ сравнением контента; после рестарта хост перерендеривал историю (workspace_info, tool-результаты) → пивот не находился → снапшот stale → полная история. **Фикс:** общий предикат `isSameConversationMessage` (cache-diagnostics.ts) используется и в стабилизации префикса, и в `messagesContentEqual`: tool_call_id / id всех tool_calls / текст с замаскированными волатильными блоками. Ходы 2+ после фикса: 99.7–100%. |
| 12 | 2026-08-07 | исправлен | Ходы 10/11/13 в чате 18a9febd: hit 0–3.7% при СОКРАЩЕНИИ контекста (420K→400K, 404K→386K); в логе `snapshot_stale reason=rewritten_history_would_duplicate` со `snapshotMessages > sourceMessages`. | 1.10.72 | **Причина:** guard считал дублированием ситуацию `snapshot.length + new > source.length + 2`, но хост САМ подрезает своё окно истории — снимок закономерно длиннее источника; guard выбрасывал валидный снапшот и отправлял полный source → префикс переписан. **Фикс:** дублирование определяется по факту повтора tool-call id (`tailRepeatsSnapshotCalls`), а не по длине. Снимок длиннее источника — норма. |
| 13 | 2026-08-07 | исправлен | Первый ход после рестарта: system-промпт «поменялся» — `firstDivergence` показал: `current: "[tool result summarized: original=38534 chars, lines=554] - You are a careful coding agent..."`. | 1.10.73 | **Причина:** `truncateToolResultMessages` усекала/суммаризовала ЛЮБОЕ сообщение длиннее maxChars (24000), включая system (~38K символов: инструкции + skills + agents + copilot-instructions). После рестарта снапшот отбрасывался, свежий source проходил через суммаризацию — system приезжал подменённым. **Фикс:** усечение только при `isToolResult` (role==="tool" либо user с маркером `[tool_result`). |
| 14 | 2026-08-07 | исправлен | Внезапный одиночный miss (2.5%) на ходе с новым сообщением пользователя (`gap=user`, ход b43b3e63): `snapshot_rebuilt` без `snapshot_stale` — пивот не найден. | 1.10.74 | **Причина:** пивот искался только по K-грамме из САМОГО хвоста снапшота; при новом сообщении пользователя хост отбрасывает свои хвостовые сообщения (прерванный ответ) → грамма не находилась → снапшот выбрасывался → полный source со свежим workspace_info. **Фикс:** `findSnapshotAlignment` отматывает пивот назад до SNAPSHOT_PIVOT_MAX_REWIND=24 сообщений, поиск по предвычисленным ключам `conversationMessageKey` + индексу позиций; лог `chat.messages.snapshot_rewound`. Диагностика `prefix.firstDivergence` показывает первое расходящееся сообщение. |
| 15 | 2026-08-07 | исправлен | Ход 50 (6eb604ea): резкий рост контекста 431K→483K, полный miss (hit 2%), `snapshot_rebuilt` без `snapshot_stale`, `ident=1`. Ходы 44–49 шли по сжатой истории (eff 420–450 при src 528–552). | 1.10.75 | **Причина:** инъекции расширения (cross-turn nudge «You have been making tool calls without a meaningful text response...», loop guard, repair/continuation-сообщения ретраев) попадали в снимки префикса и бюджета, хотя в истории хоста их нет (лог `chat.response.tool_call_only_nudge_cross_turn` перед ходом). Хвост снимка с nudge не находился в источнике → снапшот stale → полная история → miss. **Фикс:** `ephemeral?: boolean` в `OpenAIChatMessage`; все инъекции помечены, снимки фильтруют их (messageParts в stabilizeMessagePrefix + оба вызова setConversationMessageSnapshot). `snapshot_rebuilt` теперь логирует `reason` (alignment-not-found / rewritten-history-would-duplicate / migrated-from-prefix). |
| 16 | 2026-08-07 | исправлен | Первый ход после reload (17a1cc78): «+51 сообщение», hit 90.6% (uncached ~46K). Снапшот чата на диске застыл на ходе 2 (362 сообщения, 5f214c2e), хотя чат давно ушёл вперёд. | 1.10.77 | **Причина:** гонка read-modify-write в `persistSessionState` — файл `session-state.json` общий для всех окон VS Code, и каждое окно перезаписывало ВЕСЬ файл своими копиями чужих scope (загруженными при старте, устаревшими). Окно B (активно пишет каждые ~2–5 сек для своего чата) при каждой записи откатывало снапшот чата A к 362. После reload окно A загрузило устаревшие 362 → выровнялось по ним → хвост 51 сообщение из источника. **Фикс:** dirty-трекинг — persist читает текущий файл и применяет ТОЛЬКО scope, изменённые в этом extension host (dirtyPrefixScopes / dirtyConversationScopes / dirtyToolCatalogScopes); удаления снимков тоже доходят до файла. |
| 17 | 2026-08-07 | исправлен | На ходах 3–5 (после reload на 1.10.75): рост контекста +37K/ход при hit 89–97% (НЕ miss). `ident=360` — хост переписывает assistant-сообщение 360: чередуются версии «Значит g++ записал кривой...» ↔ «Странно — бинарник молчит...» (у них различаются текст и/или tool_calls → стабилизатор останавливается, хвост 360–372 с tool-результатом 141K chars переотправляется). Вероятная причина: два окна VS Code на одном чате (два лог-файла v1.10.75), VS Code материализует историю из буферов по-разному; в chatSessions полных текстов нет. | 1.10.93 | 2026-08-08: подтверждено пользователем — контекст больше не растёт. В живых логах 1.10.93 (31 ход): `snapshot_stale`=0, `snapshot_rewound`=1 (штатно, 137/148 переиспользовано), cache 36× healthy, ни одного history_rewritten. Диагностика `firstDivergence` (1.10.76) остаётся как защита на будущее. |
| 18 | 2026-08-07 | исправлен | Агент в другом окне «лупится»: серии tool-call-only ходов (outputChars=0, toolCalls=1) по 5–15 сек, nudge (4/8) периодически вытаскивает текст, но агент снова уходит в tool-only. `max_turns_stop` не срабатывает ни разу — это host-driven цикл (каждый ход = отдельный запрос VS Code), per-request лимиты его не видят. Инструменты: 63, toolExecutionErrors=0, текст бывает (до 6.7K chars) — частично легитимная работа. | 1.10.93 | **Фиксы:** `toolLoopForceTextThreshold` (default 12, 1.10.78) + устранение двойных записей reasoning в контексте (1.10.80–1.10.83, `stripReasoningDuplicatesFromContent`) — именно дабл-записи раздували контекст и провоцировали лупы. 2026-08-08: подтверждено пользователем (лупы исправлены, контекст не растёт); в живых логах 1.10.93: 0 подрядных tool-only ходов из 31, `loop_detected`=0, `tool_force_text`=0 (защита активна, не требовалась), `max_turns_stop`=0. |
| 19 | 2026-08-07 | исправлен | Мысли агента записывались в контекст ДВАЖДЫ (раздувание: в длинных сессиях до ~38K лишних токенов/запрос): сериализованный ThinkingPart из истории VS Code (plain-объект с `value`, конструктор теряется при IPC) попадал в `content` как текст — utils.ts не отличает его от text part, а `injectStoredReasoningContent` добавлял те же мысли в `reasoning_content` (требование DeepSeek при tool-call follow-up). | 1.10.80 | **Фикс:** `stripReasoningDuplicatesFromContent` (llama-provider.ts, вызывается после инъекции в convertForMode) вырезает из `content` префикс, совпадающий с `reasoning_content` (порог: ≥100 символов либо полное совпадение для коротких; `reasoning_content` сохраняется). Проверено на снапшотах session-state.json: 98/102 reasoning-сообщений с дублем; экономия 25–153K символов (~6–38K токенов) на запрос. Тест «strips reasoning the host already serialized into assistant content», 320 passing. **Дополнено в 1.10.81:** после рестарта 49/53 reasoning-сообщений оставались с дублями — strip в convertForMode чистил только свежие сообщения, а старые шли через `reusedSnapshot` в `prepareMessagesForBudget` в обход чистки. Чистка перенесена в `prepareMessagesForBudget` (после сборки `effectiveMessages`) — покрывает все сообщения запроса, включая снапшот-переиспользованные; из `convertForMode` вызов убран. **Диагностика (1.10.82):** `chat.messages.initial` → `reasoningMsgCount`/`reasoningChars`; `snapshot_rewound` → `pivotIndex`/`rewoundCount`. **Стабилизация (1.10.83):** `restoreReasoningFromSnapshot` переносит `reasoning_content` из снапшота по call id для сообщений, переписанных хостом (иначе вес контекста «плавал» ±2–4K токенов между ходами). Тест «restores reasoning from the snapshot…» (321 passing). |
| 20 | 2026-08-07 | исправлен | Контекст «плавает» ±2–4K токенов между ходами (серверные prompt-токены и uncached скачут вниз/вверх; напр. 215 816 → 211 801): контент уже отправленных сообщений менялся между запросами. | 1.10.84 | **Диагностика:** доказано по логам, что хост VS Code переписывает историю между ходами (ход 4→5: user 122→118, toolResults 106→102 — удалены 4 сообщения; ход 6→7: chars −8787 — обрезан tool-результат). При отмотке пивота (`snapshot_rewound`) хвост брался из ИСТОЧНИКА в переписанном виде (усечённые/изменённые версии заменяли полные, уже отправленные) — `findSnapshotAlignment` → `newMessages = source.slice(...)` (llama-provider.ts). **Фикс:** `stabilizeTailFromSnapshot` — сообщения, опознанные по call id как уже отправленные, возвращаются в снапшотной версии; новые (неизвестные call id) — из источника. Тест «keeps the tail stable when the host truncates messages it already sent» (322 passing). |
| 21 | 2026-08-08 | исправлен | Shared memory присутствовала в `chat.memory.context`, но периодически отсутствовала в реально отправленном prompt: ход `88fbf7f0` 192253 токена → `dc9c7b3c` 188315 при неизменном cached-префиксе 185472. | 1.10.85 | **Причина:** memory инъектировалась до snapshot alignment и на tool-ходе попадала раньше pivot, поэтому отбрасывалась из `newMessages`. Дополнительно `stabilizeMessagePrefix` фильтровал ephemeral при сериализации, но восстанавливал durable-префикс по индексам исходного нефильтрованного массива. **Фикс:** memory инъектируется после сборки durable source+snapshot; стабилизатор работает с отдельным `durableMessages` и затем реинъектирует текущие ephemeral по durable-offset. Диагностика: `ephemeralMsgCount`, `ephemeralChars`, `sharedMemoryExpected`. Красный до фикса и зелёный после него тест `keeps live shared memory while restoring a durable snapshot prefix`; 323 passing. |
| 22 | 2026-08-08 | исправлен | Error tracker: ход `d697b601` (`b32eed1e`) упал с `arguments are not a valid JSON object`; HTTP/SSE запрос был успешен, но `flushToolCallBuffers` выбросил `ToolCallValidationError`, repair не запускался. | 1.10.86 | **Причина:** `toolCallRepairEnabled=true`, лимит retry=1, но модель до malformed call успела вывести 49 символов. Условие retry требовало одновременно `roundOutputChars===0` и `roundToolCallParts===0`, поэтому частичный обычный текст необоснованно запрещал безопасную повторную попытку. **Фикс:** текст больше не блокирует bounded retry; запрет при уже отправленном tool call сохранён против двойного выполнения. Correction prompt запрещает повторять частичный текст. Диагностика: `validation_retry` получил счётчики вывода, добавлен `validation_unrecoverable`. Регрессионный тест был красным до фикса и зелёным после; 323 passing. |
| 23 | 2026-08-08 | исправлен | В 20 новых ходах `b32eed1e` compaction ни разу не запускался, хотя server prompt вырос до 260763; ходы 18–19 получили `history_rewritten` и cache hit 89.4/89.3%, хотя сообщения и размер контекста продолжали расти. | 1.10.87 | **Две причины.** (1) DeepSeek использует heuristic counter; calibration обновлялась как `EMA(previousFactor, residualRatio)` и математически сходилась к √(raw→server multiplier): factor=1.295 при реальной цели≈1.676, поэтому на ходе 20 message estimate был 196823 ниже soft target 218739. Фикс: `EMA(previousFactor, previousFactor × residualRatio)`, цель логируется как `observedFactorTarget`; тест также проверяет, что калиброванный размер запускает auto-compaction. (2) cache-prefix snapshot сохранялся до стабилизации: сообщение №255 с тем же call id/content попеременно теряло/получало `reasoning_content`, реально переписывая суффикс prompt. Теперь сохраняется и диагностируется фактически отправленный `stabilizedDurable`. Оба теста были красными до фикса и зелёными после; 325 passing. |
| 24 | 2026-08-08 | исправлен | Частичный cache miss на ходах 54/57 `b32eed1e` классифицировался как `healthy`, хотя префикс был идентичен, context рос, compaction не было, а uncached внезапно вырос до 18.6K/25.9K токенов. | 1.10.88 | **Доказательство:** miss совпал со сменой CloudFront-маршрута (`Via` и `Cf-Pop`), а следующий запрос по тому же маршруту снова получил почти полный hit. **Фикс:** `CacheBackendTelemetry` передаёт предыдущий/current route и предыдущий server prompt size; новый анализ отделяет ожидаемый прирост prompt от необъяснимого uncached-остатка (минимум 4K uncached, минимум 2K остатка и более 2× нового хвоста). Только сочетание полного prefix match + аномального остатка + route change даёт `upstream_route_changed`. Контрольный ход 56 со сменой route, но нормальным новым хвостом остаётся `healthy`; same-route anomaly даёт `upstream_expired`. Три регрессионных теста были красными до реализации и зелёными после; 328 passing. |
| 25 | 2026-08-08 | исправлен | После 1.10.88 появились вводящие в заблуждение причины: первый ход после reload назывался обычным `history_rewritten`; partial miss на том же CloudFront edge — `upstream_expired`; совпавший со сменой edge — причинно звучащим `upstream_route_changed`; первый холодный ход после compaction иногда оставался `healthy`. | 1.10.89 | **Аудит:** переиграны 50 живых cache report `b32eed1e`. Смена видимого route не доказала причинность: среди 47 переходов hit<95% встречался по 5 раз как при changed route, так и при same route; один `Via` также давал cached 213120 → 192896 → 196608. **Фикс:** прямые DeepSeek/local аномалии теперь `upstream_cache_partial`; измеряется потеря уже отправленного prefix (`previousPromptTokens-currentCachedTokens`), material threshold=max(2048, 4% prior prompt). Route упоминается только как корреляция. Добавлены `history_rebuilt_after_restart`, `ephemeral_context_changed`, надёжный `upstream_cache_pending`; prefix telemetry получила `ephemeralHash/Chars`. Replay: healthy=30, partial=12, summarized=4, pending=3, restart rebuild=1. Шесть красных регрессий + provider wiring; 334 passing. |
| 26 | 2026-08-08 | исправлен | Ход 34 `b81cc3ff` получил `ephemeral_context_changed`: при неизменной durable history потеряно 16331 cached-токенов, хотя shared memory изменилась всего 4368 → 3812 chars (3 записи/1028 токенов → 2 записи/829 токенов). | 1.10.90 | **Причина:** memory retrieval выполнялся на каждом tool-раунде. Блок вставляется перед последним настоящим user-сообщением; его небольшое изменение переписывало весь последующий assistant/tool-хвост и обрывало prefix cache на 163840 токенах. **Фикс:** `frozenSharedMemoryByScope` выбирает memory один раз на настоящий user-ход; tool-result раунды получают побайтно тот же блок, новый user-ход обновляет его. Null-sentinel не позволяет повторять retrieval при пустом результате. `chat.memory.context.source` различает `retrieved-user-turn` и `frozen-tool-turn`. Интеграционный тест был красным с memory turn two вместо turn one и зелёным после; 335 passing. |

| 27 | 2026-08-08 | исправлен | После 1.10.90 одинаковая shared memory не менялась внутри tool-цикла, но на новом user-ходе всё равно переносилась со старой позиции перед нового user, переписывая уже cached assistant/tool-хвост. Live Report скрывал это внутри общего Messages и дополнительно дважды учитывал System. | 1.10.91 | **Фикс:** append-only memory ledger: provider overlay checkpoint сохраняется на исходной позиции в conversation snapshot/session-state; alignment игнорирует overlay только как host pivot, но переносит его в отправленный prefix. Одинаковая revision ничего не добавляет; новая/обновлённая entry создаёт Memory delta перед новым некэшированным user-хвостом. Metadata удаляется из wire payload, reload/tool-turn использует persisted overlay, compaction восстанавливает актуальный checkpoint. **Диагностика:** ordered exclusive promptSegments разбивает реальный prompt на System/Tools/Memory/User/Assistant/Reasoning/Tool calls/Tool results/Summary/Guards; cached cutoff проецируется по фактическому порядку, JSON хода содержит те же сегменты. Красная регрессия на перемещение checkpoint стала зелёной; 337 passing. |

| 28 | 2026-08-08 | исправлен | Агент мог не выбрать область новой памяти: отсутствующий `scope` молча превращался в `global`, публичная схема позволяла передать произвольный workspace/model `scopeId`, а удаление выполнялось только по известному ID без проверки проекта. Это могло глобально распространить проектные команды или удалить workspace-запись из другого проекта. | 1.10.92 | Публичные tools теперь имеют ровно два scope: обязательный `global` или `workspace`; project ID вычисляется расширением и убран из input schema. `SharedMemoryService.remove` проверяет scope/workspace, store запрещает менять запись чужого проекта, model-scope оставлен только для чтения legacy данных. `chat.memory.context.scopeCounts` показывает состав выбранной памяти. Красный тест до фикса: remove принимал только ID; после фикса scoped-delete и schema regressions проходят. |

| 29 | 2026-08-08 | исправлен | В Live Report маленькие prompt/cache-сегменты почти невозможно было различить в общей полосе; одновременно раскрытая карточка хода во время model turn перескакивала на следующий ход. | 1.10.93 | Добавлен `Expand block details`: каждый ordered segment получает отдельную полноширинную cached/uncached шкалу и точные total/cached/miss/hit числа. Причина перескока — позиционные DOM ID `detail-0`: новый newest-first row забирал ID открытой записи. Detail identity переведена на `requestId`; live state отдельно сохраняет раскрытые segment details и viewport anchor, поэтому выбранная карточка остаётся той же и на том же месте экрана. UI regression + 339 passing. |

| 30 | 2026-08-08 | исправлен | Саб-агенты (`runSubagent` на моделях llama-vscode-chat) не имеют файловых инструментов: DeepSeek-саб-агент сообщал «единственный доступный тул — session_store_sql», Codex — «нет инструментов чтения/записи». | 1.11.0 | **Диагностика (логи `chat.turn.start` + `chat.request.send`):** для саб-агентских запросов VS Code передаёт `options.tools` из ОДНОГО инструмента (`advertised: 1, converted: 1`, в requestBody `tools.names: ["session_store_sql"]`), обычный чат получает 64. Сужение происходит в бандле Copilot (0.59.0) на уровне `getAvailableTools`/`request.tools` кастомных root-агентов (`tools: void 0`). **Фикс (copilot-patch v21+, `src/copilot-patch.ts`):** в `getAvailableTools` обычные ходы кэшируют полный набор в `this._llamaFullTools`, саб-агентский ход (`subAgentInvocationId`) подменяет узкий набор закэшированным полным — саб-агент получает тот же набор тулов, что и вызывающий агент (`advertisedToolCount` 1 → 64). Тест `copilot-patch.test.ts` (маркер + `new Script` валидность), 346 passing. |

| 19 | 2026-08-07 | исправлен | Риск «дополнительных сообщений» в контексте: memory-инъекция (`injectSharedMemoryContext`) вставляла отдельное user-сообщение с блоком памяти в каждый запрос БЕЗ флага ephemeral — блок попадал в снапшоты префикса/бюджета и «плавал» при изменении записей памяти между ходами → расхождение префикса → miss. | 1.10.79 | **Фикс:** memory-сообщение помечено `ephemeral: true` (src/memory/prompt.ts) — остаётся в запросе, исключено из снапшотов (фильтры 1.10.75). Остальные инъекции (nudge, loop guard, force-text, repair/continuation) уже были ephemeral — накопления в контексте нет. **Заменено в 1.10.91:** memory стала append-only provider overlay, а ephemeral остался только у конечных guards/nudges. |
