{
	"name": "llama-vscode-chat",
	"publisher": "mrlordcat",
	"displayName": "AI Agent Bridge",
	"description": "Bridge local models, DeepSeek, Codex, and Claude subscriptions into native VS Code Chat with tools, subagents, memory, context control, and diagnostics.",
	"icon": "assets/logo.png",
	"repository": {
		"type": "git",
		"url": "https://github.com/MrLordCat/ai-agent-bridge.git"
	},
	"version": "1.14.35",
	"engines": {
		"vscode": "^1.131.0",
		"node": ">=20"
	},
	"categories": [
		"AI",
		"Chat"
	],
	"galleryBanner": {
		"color": "#100f11",
		"theme": "dark"
	},
	"badges": [],
	"homepage": "https://github.com/MrLordCat/ai-agent-bridge",
	"bugs": {
		"url": "https://github.com/MrLordCat/ai-agent-bridge/issues"
	},
	"license": "MIT",
	"activationEvents": [
		"onStartupFinished",
		"onLanguageModelChatProvider:llamacpp"
	],
	"contributes": {
		"languageModelChatProviders": [
			{
				"vendor": "llamacpp",
				"displayName": "AI Agent Bridge",
				"managementCommand": "llamacpp.manage"
			}
		],
		"viewsContainers": {
			"activitybar": [
				{
					"id": "llamacpp-sidebar",
					"title": "AI Agent Bridge",
					"icon": "assets/activity-icon.svg"
				}
			]
		},
		"views": {
			"llamacpp-sidebar": [
				{
					"id": "llamacpp-quick-actions",
					"name": "Quick Access",
					"visibility": "visible"
				}
			]
		},
		"configuration": {
			"title": "AI Agent Bridge",
			"properties": {
				"llamacpp.serverUrl": {
					"type": "string",
					"default": "http://localhost:8000",
					"description": "Primary OpenAI-compatible server URL. Kept for backward compatibility; local and DeepSeek sources can also be enabled separately."
				},
				"llamacpp.enableLocalServer": {
					"type": "boolean",
					"default": true,
					"description": "Also advertise models from a local OpenAI-compatible llama.cpp server without switching the primary server URL."
				},
				"llamacpp.localServerUrl": {
					"type": "string",
					"default": "http://localhost:8000",
					"description": "Local OpenAI-compatible llama.cpp server URL advertised alongside DeepSeek or the primary server."
				},
				"llamacpp.localContextLength": {
					"type": "number",
					"default": 65536,
					"minimum": 4096,
					"maximum": 1048576,
					"description": "Fallback context length for the dedicated local server source when runtime metadata is unavailable."
				},
				"llamacpp.enableDeepSeek": {
					"type": "boolean",
					"default": true,
					"description": "Also advertise DeepSeek models from https://api.deepseek.com when an API key is configured."
				},
				"llamacpp.deepSeekContextLength": {
					"type": "number",
					"default": 258400,
					"minimum": 32768,
					"maximum": 1048576,
					"description": "Maximum context window advertised for DeepSeek models. This is independent of the local llama.cpp/Qwen server context."
				},
				"llamacpp.enableCodexSubscription": {
					"type": "boolean",
					"default": true,
					"description": "Advertise OpenAI Codex models backed by the ChatGPT account managed by the official local Codex app-server."
				},
				"llamacpp.codexCliPath": {
					"type": "string",
					"default": "",
					"description": "Optional path to the Codex CLI executable. Empty uses the CLI bundled with the official OpenAI VS Code extension, then falls back to codex on PATH."
				},
				"llamacpp.codexContextLength": {
					"type": "number",
					"default": 258400,
					"minimum": 32768,
					"maximum": 1048576,
					"description": "Fallback Codex model window before app-server reports the real value. Server telemetry takes precedence; changing this value cannot enlarge the provider limit."
				},
				"llamacpp.codexWorkingContextTarget": {
					"type": "number",
					"default": 258400,
					"minimum": 131072,
					"maximum": 1048576,
					"description": "Total Codex working-context target used for cold-start compaction. It is capped to the server-reported model window, then output, tool-schema, developer-instruction, and safety reserves are subtracted."
				},
				"llamacpp.codexCompactKeepLastTurns": {
					"type": "number",
					"default": 18,
					"minimum": 2,
					"maximum": 64,
					"description": "Number of recent VS Code conversation entries preserved verbatim when older Codex history is compacted into a summary."
				},
				"llamacpp.codexMaxInputChars": {
					"type": "number",
					"default": 900000,
					"minimum": 65536,
					"maximum": 4000000,
					"description": "Maximum serialized VS Code conversation characters sent in one Codex turn. Older middle messages are omitted first, leaving headroom below the Codex app-server hard input limit."
				},
				"llamacpp.codexMaxToolResultChars": {
					"type": "number",
					"default": 12000,
					"minimum": 1024,
					"maximum": 100000,
					"description": "Maximum characters retained from each historical VS Code tool result before Codex conversation serialization. Preserves more user and assistant history in long chats."
				},
				"llamacpp.codexDeferNonCoreTools": {
					"type": "boolean",
					"default": true,
					"description": "Keep core coding tools immediately visible to Codex and load uncommon Copilot tool schemas on demand through Codex tool search. Reduces repeated prompt tokens when many tools are installed."
				},
				"llamacpp.codexMaxOutputTokens": {
					"type": "number",
					"default": 32768,
					"minimum": 1024,
					"maximum": 131072,
					"description": "Output reserve advertised to VS Code for Codex models. Codex still applies the selected model and subscription limits."
				},
				"llamacpp.codexReasoningEffort": {
					"type": "string",
					"enum": [
						"auto",
						"low",
						"medium",
						"high",
						"xhigh",
						"max",
						"ultra"
					],
					"default": "auto",
					"description": "Default Codex reasoning effort. Auto prefers High when the selected model supports it; the native per-chat Thinking Effort selector takes precedence."
				},
				"llamacpp.codexReasoningSummary": {
					"type": "string",
					"enum": [
						"auto",
						"concise",
						"detailed",
						"none"
					],
					"default": "auto",
					"description": "Reasoning summary detail streamed by Codex into the native thinking section when supported by VS Code."
				},
				"llamacpp.codexFastServiceTier": {
					"type": "boolean",
					"default": false,
					"description": "Use the Codex priority service tier when the selected model offers it. This is faster but consumes subscription usage more quickly."
				},
				"llamacpp.persistProviderSessions": {
					"type": "boolean",
					"default": true,
					"description": "Resume completed Claude and Codex conversations across VS Code reloads. Only an explicit false value disables persistence; contributed defaults from an older installed manifest are ignored. Claude stores its transcript through the official Agent SDK; VS Code workspace state stores only provider session ids and validation fingerprints. Local and DeepSeek remain stateless and reuse their stable server-side prompt-cache prefix."
				},
				"llamacpp.codexEphemeralThreads": {
					"type": "boolean",
					"default": false,
					"description": "Force Codex app-server threads to remain in-memory only. Enable for maximum local privacy; this disables Codex resume across VS Code reloads even when persistProviderSessions is enabled."
				},
				"llamacpp.enableClaudeSubscription": {
					"type": "boolean",
					"default": true,
					"description": "Advertise Claude subscription models through the official Claude Agent SDK. Built-in Claude Code tools are disabled; actions are delegated to native VS Code Chat tools."
				},
				"llamacpp.claudeContextLength": {
					"type": "number",
					"default": 258400,
					"minimum": 258400,
					"maximum": 967000,
					"description": "Claude Opus 5 working-context target. The provider selects the real 1M runtime and sets Claude Code auto-compaction to this value; 967k leaves provider headroom below the raw 1M window."
				},
				"llamacpp.claudeMaxOutputTokens": {
					"type": "number",
					"default": 32000,
					"minimum": 1024,
					"maximum": 32768,
					"description": "Maximum output tokens advertised for Claude models. The default matches the backend-reported Opus 5 limit; Anthropic still applies live per-model limits."
				},
				"llamacpp.claudeMaxInputChars": {
					"type": "number",
					"default": 4000000,
					"minimum": 32768,
					"maximum": 4000000,
					"description": "Hard character cap for initializing a new Claude session. The provider also derives a lower safe cap from the selected working-context target; warm turns send only the latest user message."
				},
				"llamacpp.claudeReasoningEffort": {
					"type": "string",
					"enum": [
						"auto",
						"low",
						"medium",
						"high",
						"xhigh",
						"max"
					],
					"default": "auto",
					"description": "Default Claude reasoning effort. The Opus 5 model picker profile exposes native per-chat Low, Medium, High, Extra High, and Max choices."
				},
				"llamacpp.claudeCliPath": {
					"type": "string",
					"default": "",
					"description": "Absolute path to the Claude Code CLI binary. Leave empty to use the binary bundled with the official 'anthropic.claude-code' extension."
				},
				"llamacpp.claudeCacheKeepAliveEnabled": {
					"type": "boolean",
					"default": true,
					"description": "Keep the Anthropic prompt cache warm while you are idle. A minimal turn refreshes the largest eligible prefix above 100k tokens. Pauses when the 5-hour usage snapshot is unknown, stale, or at least 90% used. Runtime state and the last result are visible in Session Quality."
				},
				"llamacpp.claudeCacheKeepAliveMs": {
					"type": "number",
					"default": 2700000,
					"minimum": 60000,
					"maximum": 3600000,
					"description": "Interval between Claude cache keep-alive turns (45 minutes by default, inside the 1-hour Anthropic cache TTL)."
				},
				"llamacpp.claudeCacheKeepAliveIgnoreUsageLimit": {
					"type": "boolean",
					"default": false,
					"description": "When enabled, the cache keep-alive continues even when the 5-hour usage limit reaches the 90% auto-pause threshold. Use when you need the prefix cache to survive across the usage-limit reset."
				},
				"llamacpp.codexCacheKeepAliveEnabled": {
					"type": "boolean",
					"default": false,
					"description": "Keep the Codex server-side prompt cache warm while you are idle. A minimal turn refreshes the largest eligible thread prefix. Pauses when the ChatGPT subscription usage snapshot is unknown, stale, or at least 90% used, and when any Codex turn is active. Disabled by default because Codex is billed per token."
				},
				"llamacpp.codexCacheKeepAliveMs": {
					"type": "number",
					"default": 2700000,
					"minimum": 60000,
					"maximum": 3600000,
					"description": "Interval between Codex cache keep-alive turns (45 minutes by default)."
				},
				"llamacpp.codexCacheKeepAliveIgnoreUsageLimit": {
					"type": "boolean",
					"default": false,
					"description": "When enabled, the Codex cache keep-alive continues even when the subscription usage limit reaches the 90% auto-pause threshold."
				},
				"llamacpp.claudeResumeFallbackPolicy": {
					"type": "string",
					"enum": [
						"safe",
						"never",
						"always"
					],
					"default": "safe",
					"description": "Controls automatic full-input replay after a durable Claude resume fails. Safe requires a small replay and a fresh low five-hour usage snapshot; never blocks all automatic cold replays; always disables these protections."
				},
				"llamacpp.claudeResumeFallbackMaxInputTokens": {
					"type": "number",
					"default": 64000,
					"minimum": 0,
					"maximum": 1000000,
					"description": "Maximum estimated input tokens, including tool schemas, that safe mode may automatically cold-replay after Claude resume failure."
				},
				"llamacpp.claudeResumeFallbackMaxUsagePercent": {
					"type": "number",
					"default": 80,
					"minimum": 0,
					"maximum": 100,
					"description": "Maximum fresh Claude five-hour usage percentage below which safe mode may automatically cold-replay after resume failure."
				},
				"llamacpp.claudeMaxAgentTurns": {
					"type": "number",
					"default": 0,
					"minimum": 0,
					"maximum": 200,
					"description": "Maximum Claude Agent SDK model segments in one outer VS Code turn. Set to 0 to disable this segment cap; the cumulative-input circuit breaker remains active."
				},
				"llamacpp.workbenchTerminalPatchEnabled": {
					"type": "boolean",
					"default": true,
					"description": "Reuse an idle background tool terminal for sync commands instead of spawning a new terminal per call (patches the VS Code workbench bundle; requires a window restart to take effect)."
				},
				"llamacpp.claudeMaxCumulativeInputTokens": {
					"type": "number",
					"default": 10000000,
					"minimum": 100000,
					"maximum": 50000000,
					"description": "Maximum cumulative Claude input tokens processed across all model segments in one outer VS Code turn."
				},
				"llamacpp.contextLength": {
					"type": "number",
					"default": 65536,
					"minimum": 4096,
					"maximum": 1048576,
					"description": "Optional runtime context override, for example --ctx-size 65536. When not explicitly set, the provider infers context from runtime metadata (for example /slots on llama.cpp or /models metadata on DeepSeek)."
				},
				"llamacpp.modelFamily": {
					"type": "string",
					"default": "llama",
					"description": "Model family advertised to VS Code Chat. Use auto to infer from model id, or set a value like qwen, llama, mistral, deepseek, gemma, or phi."
				},
				"llamacpp.modelListCacheTtlMs": {
					"type": "number",
					"default": 30000,
					"minimum": 0,
					"maximum": 600000,
					"description": "How long to cache model discovery results in milliseconds. Set to 0 to disable model list caching."
				},
				"llamacpp.modelDiscoveryTimeoutMs": {
					"type": "number",
					"default": 20000,
					"minimum": 3000,
					"maximum": 120000,
					"description": "Timeout for model discovery calls (/v1/models or /models, plus runtime context probing) in milliseconds. Lower this if model loading appears to hang on remote endpoints."
				},
				"llamacpp.autoCompact": {
					"type": "boolean",
					"default": false,
					"description": "Compact chat history proactively before reaching the context limit. Disabled by default: history is sent as-is and compaction only happens when the server confirms a context overflow (see retryOnContextOverflow), avoiding cache-prefix rewrites from double compaction."
				},
				"llamacpp.compactionTargetRatio": {
					"type": "number",
					"default": 0.75,
					"minimum": 0.25,
					"maximum": 0.9,
					"description": "Share of the current message context retained when proactive or overflow compaction runs. Set 0.25 to retain about 25% and free roughly 75%; extreme targets work best with DeepSeek AI summaries and may keep less raw recent history."
				},
				"llamacpp.deepSeekCompactionSummary": {
					"type": "boolean",
					"default": false,
					"description": "Use deepseek-v4-flash to generate a semantic summary when the extension compacts local or DeepSeek HTTP chat history. Each compaction makes a separate paid DeepSeek API request. Disabled by default; failures safely fall back to deterministic local summarization. Claude and Codex keep their provider-native compaction paths."
				},
				"llamacpp.accurateTokenCounting": {
					"type": "boolean",
					"default": true,
					"description": "Use llama.cpp /apply-template and /tokenize for model-exact prompt counts. Falls back to local estimation when unsupported."
				},
				"llamacpp.tokenizerTimeoutMs": {
					"type": "number",
					"default": 10000,
					"minimum": 1000,
					"maximum": 30000,
					"description": "Timeout for non-generating /apply-template and /tokenize requests used by accurate context accounting."
				},
				"llamacpp.retryOnContextOverflow": {
					"type": "boolean",
					"default": true,
					"description": "Retry once with hard compaction when server reports context overflow."
				},
				"llamacpp.emptyResponseAutoRetry": {
					"type": "boolean",
					"default": true,
					"description": "Automatically retry with an internal continuation prompt when a turn ends with no visible text and no tool calls."
				},
				"llamacpp.emptyResponseAutoRetryMaxAttempts": {
					"type": "number",
					"default": 1,
					"minimum": 0,
					"maximum": 3,
					"description": "Maximum automatic continuation retries for empty model responses per turn."
				},
				"llamacpp.compactMaxToolResultChars": {
					"type": "number",
					"default": 8000,
					"minimum": 1000,
					"maximum": 24000,
					"description": "Maximum characters kept for each tool result when compaction retains history. Long tool outputs are truncated to this size so more recent turns fit inside the compaction budget instead of being dropped entirely."
				},
				"llamacpp.toolCallOnlyAutoretry": {
					"type": "boolean",
					"default": true,
					"description": "Within a single provider request, recover from several tool-call-only generations by asking for the next useful tool call. Normal tool-only turns across VS Code requests remain uninterrupted and never trigger a progress review."
				},
				"llamacpp.toolCallOnlyAutoretryThreshold": {
					"type": "number",
					"default": 3,
					"minimum": 2,
					"maximum": 10,
					"description": "Number of consecutive tool-call-only generations within one provider request that triggers the continue-execution recovery prompt."
				},
				"llamacpp.toolLoopForceTextThreshold": {
					"type": "number",
					"default": 12,
					"minimum": 6,
					"maximum": 40,
					"description": "Deprecated compatibility setting. Productive cross-request tool flows no longer disable tools based only on their length; actual identical-call loops are handled by toolLoopProtection.",
					"deprecationMessage": "No longer used. Configure toolLoopProtection and toolLoopDetectionThreshold for actual repeated-call loops."
				},
				"llamacpp.toolCallRepairEnabled": {
					"type": "boolean",
					"default": true,
					"description": "Repair conservative tool-call JSON/name mistakes and retry one rejected tool call before failing the chat turn."
				},
				"llamacpp.validateToolCallSchema": {
					"type": "boolean",
					"default": true,
					"description": "Validate model tool-call arguments against the exact schema advertised in the request before VS Code executes the tool."
				},
				"llamacpp.toolCallRepairMaxAttempts": {
					"type": "number",
					"default": 1,
					"minimum": 0,
					"maximum": 2,
					"description": "Maximum model correction retries after a malformed, unknown, or schema-invalid tool call."
				},
				"llamacpp.toolLoopProtection": {
					"type": "boolean",
					"default": true,
					"description": "Detect consecutive identical tool calls in conversation history and instruct the model to change approach instead of repeating them."
				},
				"llamacpp.toolLoopDetectionThreshold": {
					"type": "number",
					"default": 3,
					"minimum": 2,
					"maximum": 10,
					"description": "Number of consecutive identical tool calls that activates loop protection."
				},
				"llamacpp.reasoningLoopProtection": {
					"type": "boolean",
					"default": true,
					"description": "Stop exact multi-kilobyte repetition loops in streamed model reasoning, rebuild a clean provider summary, and retry once before returning a bounded stop message."
				},
				"llamacpp.reasoningLoopMinChars": {
					"type": "number",
					"default": 4096,
					"minimum": 2048,
					"maximum": 32768,
					"description": "Minimum streamed reasoning volume before exact periodic repetition detection can stop a response. Larger values reduce sensitivity for highly repetitive technical output."
				},
				"llamacpp.reasoningLoopRetryMaxAttempts": {
					"type": "number",
					"default": 1,
					"minimum": 0,
					"maximum": 2,
					"description": "Maximum clean-context retries after a streamed reasoning repetition loop. A retry summarizes the old chain and keeps only an internal recovery instruction as raw history."
				},
				"llamacpp.maxModelTurnsPerRequest": {
					"type": "number",
					"default": 6,
					"minimum": 2,
					"maximum": 20,
					"description": "Hard cap on model rounds per single chat request. Stops requests that keep retrying (empty responses, tool-call-only nudges, tool-call repairs) without producing a final answer."
				},
				"llamacpp.emptyResponseContinuationPrompt": {
					"type": "string",
					"default": "Continue from your previous response and complete the answer. Do not repeat already completed parts.",
					"description": "Internal prompt appended for empty-response auto-retry continuation."
				},
				"llamacpp.contextUtilization": {
					"type": "number",
					"default": 0.94,
					"minimum": 0.5,
					"maximum": 0.95,
					"description": "Soft context budget ratio used before compaction."
				},
				"llamacpp.hardContextUtilization": {
					"type": "number",
					"default": 0.72,
					"minimum": 0.4,
					"maximum": 0.9,
					"description": "Diagnostic only: lower reference point for the hard input target shown in logs and the dashboard. Compaction itself uses a single soft scheme (see autoCompact)."
				},
				"llamacpp.compactKeepLastTurns": {
					"type": "number",
					"default": 12,
					"minimum": 2,
					"maximum": 64,
					"description": "Number of most recent non-system messages to keep during compaction."
				},
				"llamacpp.agentHistoryRounds": {
					"type": "number",
					"default": 400,
					"minimum": 0,
					"maximum": 2000,
					"description": "Cap for agent tool-call rounds rendered into the prompt for llama.cpp models. The oldest 20% plus the newest rounds are kept; 0 disables the cap. Large conversations make Copilot's prompt renderer spend minutes per step even when tokens are cached."
				},
				"llamacpp.agentHistoryTurns": {
					"type": "number",
					"default": 80,
					"minimum": 0,
					"maximum": 2000,
					"description": "Cap for previous conversation turns rendered into the prompt for llama.cpp models. The oldest 20% plus the newest turns are kept, and the drop point moves in steps so the upstream prompt cache stays valid. 0 restores the default."
				},
				"llamacpp.maxOutputTokensCap": {
					"type": "number",
					"default": 131072,
					"minimum": 128,
					"maximum": 393216,
					"description": "Hard cap for max_tokens sent to server. Set to 393216 for maximum DeepSeek V4 output."
				},
				"llamacpp.localDefaultMaxOutputTokens": {
					"type": "number",
					"default": 32768,
					"minimum": 1024,
					"maximum": 131072,
					"description": "Default max_tokens requested for local models when the chat session does not provide an explicit limit. The global maxOutputTokensCap remains the hard ceiling."
				},
				"llamacpp.deepSeekDefaultMaxOutputTokens": {
					"type": "number",
					"default": 70000,
					"minimum": 1024,
					"maximum": 393216,
					"description": "Default max_tokens requested from DeepSeek when the chat session does not provide an explicit limit. 70000 balances long autonomous work against cost and loop risk (reasoning tokens count toward the output budget); raise only if long answers get cut."
				},
				"llamacpp.minReplyReserveTokens": {
					"type": "number",
					"default": 1536,
					"minimum": 256,
					"maximum": 32768,
					"description": "Minimum tokens reserved for reply budget when calculating input context budget."
				},
				"llamacpp.replyReservePercent": {
					"type": "number",
					"default": 0.07,
					"minimum": 0.03,
					"maximum": 0.25,
					"description": "Maximum fraction of total context window to reserve for model reply. Prevents over-reserving for models with large max_tokens. 0.07 = 7%."
				},
				"llamacpp.maxToolsPerRequest": {
					"type": "number",
					"default": 128,
					"minimum": 0,
					"maximum": 128,
					"description": "Maximum number of tools allowed in a single request for classic mode. In apiDirect mode, request-level cap is bypassed and apiDirectMaxTools is used."
				},
				"llamacpp.requestTimeoutMs": {
					"type": "number",
					"default": 1200000,
					"minimum": 10000,
					"maximum": 1200000,
					"description": "HTTP timeout for chat completion requests in milliseconds."
				},
				"llamacpp.requestQueueTimeoutMs": {
					"type": "number",
					"default": 1200000,
					"minimum": 0,
					"maximum": 1200000,
					"description": "Maximum time a chat request can wait for the local llama.cpp request slot. Set to 0 to wait indefinitely."
				},
				"llamacpp.transientRetryMaxAttempts": {
					"type": "number",
					"default": 2,
					"minimum": 0,
					"maximum": 3,
					"description": "Additional pre-stream retries for network failures and HTTP 429, 502, 503, or 504 responses. Timeout and cancellation are never retried."
				},
				"llamacpp.transientRetryBaseDelayMs": {
					"type": "number",
					"default": 500,
					"minimum": 100,
					"maximum": 10000,
					"description": "Base exponential backoff delay for transient request retries. Retry-After takes precedence when supplied by the server."
				},
				"llamacpp.cachePrompt": {
					"type": "boolean",
					"default": true,
					"description": "Send cache_prompt=true to llama.cpp so repeated prompt prefixes can reuse the server-side KV cache when supported."
				},
				"llamacpp.deepSeekCacheWriteGraceMs": {
					"type": "number",
					"default": 60000,
					"minimum": 0,
					"maximum": 600000,
					"description": "After a DeepSeek auto-compaction, wait up to this long before sending the next tool-result continuation. DeepSeek materializes the new disk-cache prefix asynchronously, so an immediate continuation misses even with an identical prefix. Lower values trade a rare extra uncached rewrite for less idle time. 0 disables the wait."
				},
				"llamacpp.maxToolResultChars": {
					"type": "number",
					"default": 24000,
					"minimum": 0,
					"maximum": 1000000,
					"description": "Maximum characters per tool result sent to the model. Large tool outputs are truncated before they can overwhelm local context. Set to 0 to disable truncation."
				},
				"llamacpp.summarizeLargeToolResults": {
					"type": "boolean",
					"default": true,
					"description": "When enabled, oversized tool results are replaced with a compact summary + preview instead of raw truncated payload. Helps reduce JSON/log bleed-through in model context."
				},
				"llamacpp.sanitizeToolResultArtifacts": {
					"type": "boolean",
					"default": true,
					"description": "Strip known transient transport artifacts from tool output text (for example cache_control metadata blobs) before sending to the model."
				},
				"llamacpp.toolCallingMode": {
					"type": "string",
					"enum": [
						"classic",
						"apiDirect"
					],
					"default": "apiDirect",
					"description": "Tool transport strategy: classic sends full tool schemas, apiDirect sends a compact prioritized subset to reduce token cost."
				},
				"llamacpp.apiDirectMaxTools": {
					"type": "number",
					"default": 100,
					"minimum": 1,
					"maximum": 128,
					"description": "Maximum number of prioritized tools included when toolCallingMode=apiDirect."
				},
				"llamacpp.apiDirectIncludeAllTools": {
					"type": "boolean",
					"default": false,
					"description": "When toolCallingMode=apiDirect, include all advertised tools (up to apiDirectMaxTools) instead of prioritized subset."
				},
				"llamacpp.apiDirectToolTokenBudget": {
					"type": "number",
					"default": 12000,
					"minimum": 256,
					"maximum": 65536,
					"description": "Approximate token ceiling for compact apiDirect tool definitions. High-priority tools are kept first."
				},
				"llamacpp.knowledgeMode": {
					"type": "string",
					"enum": [
						"off",
						"adaptive",
						"strict"
					],
					"default": "adaptive",
					"description": "Knowledge verification policy: adaptive checks material changing or uncertain claims, strict requires source-backed verification for external technical claims, and off disables the built-in policy."
				},
				"llamacpp.customSystemPrompt": {
					"type": "string",
					"default": "",
					"markdownDescription": "Optional stable instructions appended to the provider system policy. Keep frequently changing task context in chat or memory so the prompt prefix remains cache-friendly."
				},
				"llamacpp.thinkingMode": {
					"type": "string",
					"enum": [
						"auto",
						"off",
						"light",
						"balanced",
						"deep"
					],
					"default": "auto",
					"description": "Reasoning profile for Qwen/llama.cpp requests."
				},
				"llamacpp.reasoningBudget": {
					"type": "number",
					"default": 16384,
					"minimum": 256,
					"maximum": 65536,
					"description": "Maximum hidden reasoning tokens for compatible local servers. light uses up to 512, balanced up to 2048, and high/deep/auto use this cap. DeepSeek uses high/max effort instead of this numeric value."
				},
				"llamacpp.preserveThinking": {
					"type": "boolean",
					"default": true,
					"description": "Preserve Qwen 3.6 reasoning traces across multi-step tool turns when supported by the server chat template. Disable to minimize context use."
				},
				"llamacpp.toolResultMode": {
					"type": "string",
					"enum": [
						"auto",
						"tool",
						"user"
					],
					"default": "auto",
					"description": "How tool results are sent back to model context: auto (prefer tool role, fallback to user), tool (always OpenAI tool role), user (always flattened user text)."
				},
				"llamacpp.memoryEnabled": {
					"type": "boolean",
					"default": true,
					"description": "Enable shared durable memory across chats, workspaces, local models, and DeepSeek."
				},
				"llamacpp.memoryAutoInject": {
					"type": "boolean",
					"default": true,
					"description": "Automatically add relevant shared memory entries to each model request."
				},
				"llamacpp.memoryMaxTokens": {
					"type": "number",
					"default": 4096,
					"minimum": 128,
					"maximum": 32768,
					"description": "Maximum estimated tokens reserved for automatically injected shared memory."
				},
				"llamacpp.autoPatchCopilot": {
					"type": "boolean",
					"default": true,
					"description": "Automatically verify and apply the guarded Copilot Chat integration patch on startup. A reload prompt appears only when the active VS Code bundle was actually changed."
				},
				"llamacpp.enableFileLogging": {
					"type": "boolean",
					"default": true,
					"description": "Write detailed provider logs to a JSONL file in the extension global storage."
				},
				"llamacpp.logStreamChunks": {
					"type": "boolean",
					"default": false,
					"description": "Include streaming response chunks in log files. Disabled by default because chunk logging duplicates generated text and adds overhead during long local generations."
				},
				"llamacpp.maxLoggedStreamChunkChars": {
					"type": "number",
					"default": 4096,
					"minimum": 0,
					"maximum": 100000,
					"description": "Maximum characters of each stream chunk to write when logStreamChunks is enabled. Set to 0 to log chunk metadata without text."
				},
				"llamacpp.showPerformanceStatusBar": {
					"type": "boolean",
					"default": true,
					"description": "Show a status bar item with estimated llama.cpp throughput (tok/s) and turn latency from the latest chat response."
				},
				"llamacpp.showContextUsageStatusBar": {
					"type": "boolean",
					"default": true,
					"description": "Show a status bar item with the latest llama.cpp context usage estimate and budget breakdown."
				},
				"llamacpp.maxLogFiles": {
					"type": "number",
					"default": 20,
					"minimum": 1,
					"maximum": 200,
					"description": "Maximum number of JSONL log files kept in the logs directory. Older files are removed automatically."
				}
			}
		},
		"languageModelTools": [
			{
				"name": "llamacpp_store_memory",
				"displayName": "Store Shared Memory",
				"toolReferenceName": "storeSharedMemory",
				"modelDescription": "Create or update a durable user-approved memory entry. You MUST choose scope explicitly: global only for information useful to agents in every project, or workspace for information useful only in the current project. Prefer workspace for repository paths, commands, architecture, decisions, and project-specific environment facts. Use global for stable cross-project user preferences and reusable workflows. Do not store secrets, transient chat details, or guesses.",
				"userDescription": "Save durable context globally or only for the current project.",
				"canBeReferencedInPrompt": true,
				"icon": "$(database)",
				"tags": [
					"memory",
					"llamacpp"
				],
				"inputSchema": {
					"type": "object",
					"properties": {
						"id": {
							"type": "string",
							"description": "Existing memory entry id to update. Omit to create a new entry."
						},
						"title": {
							"type": "string",
							"description": "Short descriptive title for the durable fact or decision."
						},
						"content": {
							"type": "string",
							"description": "Self-contained memory content that remains understandable in a future chat."
						},
						"tags": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Optional lowercase search tags."
						},
						"pinned": {
							"type": "boolean",
							"description": "Prioritize this entry after it passes scope, expiry, and relevance checks."
						},
						"scope": {
							"type": "string",
							"enum": [
								"global",
								"workspace"
							],
							"description": "Required destination. Use global only when useful in every project; use workspace for the current project. The workspace identity is filled securely by the extension."
						},
						"kind": {
							"type": "string",
							"enum": [
								"preference",
								"decision",
								"environment",
								"workflow",
								"externalFact",
								"other"
							],
							"description": "Semantic type. externalFact requires sourceUrl and verifiedAt."
						},
						"sourceUrl": {
							"type": "string",
							"description": "Primary source URL for an externally verified fact."
						},
						"verifiedAt": {
							"type": "string",
							"description": "ISO date/time when the external fact was verified."
						},
						"expiresAt": {
							"type": "string",
							"description": "Optional ISO date/time after which the entry is excluded from automatic retrieval."
						}
					},
					"required": [
						"title",
						"content",
						"scope"
					],
					"additionalProperties": false
				}
			},
			{
				"name": "llamacpp_search_memory",
				"displayName": "Search Shared Memory",
				"toolReferenceName": "searchSharedMemory",
				"modelDescription": "Search durable memory visible to the current project. Without scope, results include global memory plus memory belonging to the current project. Set global or workspace to restrict the search. An empty query lists recent and pinned entries.",
				"userDescription": "Find durable context saved by this extension.",
				"canBeReferencedInPrompt": true,
				"icon": "$(search)",
				"tags": [
					"memory",
					"llamacpp"
				],
				"inputSchema": {
					"type": "object",
					"properties": {
						"query": {
							"type": "string",
							"description": "Keywords describing the memory to retrieve."
						},
						"limit": {
							"type": "number",
							"minimum": 1,
							"maximum": 50,
							"description": "Maximum number of entries to return."
						},
						"includeExpired": {
							"type": "boolean",
							"description": "Include expired entries for maintenance and review."
						},
						"scope": {
							"type": "string",
							"enum": [
								"global",
								"workspace"
							],
							"description": "Optionally restrict results to global memory or memory owned by the current project."
						}
					},
					"additionalProperties": false
				}
			},
			{
				"name": "llamacpp_delete_memory",
				"displayName": "Delete Shared Memory",
				"toolReferenceName": "deleteSharedMemory",
				"modelDescription": "Delete one durable memory entry by exact id and explicit scope when it is obsolete or incorrect. Choose global only for a global entry, or workspace for an entry belonging to the current project. Never use this tool speculatively.",
				"userDescription": "Remove an obsolete durable memory entry.",
				"canBeReferencedInPrompt": true,
				"icon": "$(trash)",
				"tags": [
					"memory",
					"llamacpp"
				],
				"inputSchema": {
					"type": "object",
					"properties": {
						"id": {
							"type": "string",
							"description": "Exact id of the memory entry to delete."
						},
						"scope": {
							"type": "string",
							"enum": [
								"global",
								"workspace"
							],
							"description": "Required scope of the entry. Workspace deletion is restricted to the current project."
						}
					},
					"required": [
						"id",
						"scope"
					],
					"additionalProperties": false
				}
			}
		],
		"commands": [
			{
				"command": "llamacpp.openSidebar",
				"title": "AI Agent Bridge: Open Sidebar"
			},
			{
				"command": "llamacpp.manage",
				"title": "AI Agent Bridge: Manage Primary Server"
			},
			{
				"command": "llamacpp.setLocalServerUrl",
				"title": "AI Agent Bridge: Set Local Server URL"
			},
			{
				"command": "llamacpp.toggleLocalServer",
				"title": "AI Agent Bridge: Toggle Local Server Source"
			},
			{
				"command": "llamacpp.toggleDeepSeek",
				"title": "AI Agent Bridge: Toggle DeepSeek Source"
			},
			{
				"command": "llamacpp.toggleDeepSeekCompactionSummary",
				"title": "AI Agent Bridge: Toggle DeepSeek Compaction Summaries"
			},
			{
				"command": "llamacpp.configureDeepSeek",
				"title": "AI Agent Bridge: Configure DeepSeek"
			},
			{
				"command": "llamacpp.setDeepSeekContextLength",
				"title": "AI Agent Bridge: Set DeepSeek Maximum Context"
			},
			{
				"command": "llamacpp.toggleCodexSubscription",
				"title": "AI Agent Bridge: Toggle Codex Subscription Source"
			},
			{
				"command": "llamacpp.toggleCodexDeferredTools",
				"title": "AI Agent Bridge: Toggle Codex Deferred Tools"
			},
			{
				"command": "llamacpp.codexSignIn",
				"title": "AI Agent Bridge: Sign In to Codex Subscription"
			},
			{
				"command": "llamacpp.codexSignOut",
				"title": "AI Agent Bridge: Sign Out of Codex Subscription"
			},
			{
				"command": "llamacpp.codexShowStatus",
				"title": "AI Agent Bridge: Show Codex Subscription Status"
			},
			{
				"command": "llamacpp.codexContinueLatestInNewChat",
				"title": "AI Agent Bridge: Continue Latest Codex Thread in New Chat"
			},
			{
				"command": "llamacpp.toggleClaudeSubscription",
				"title": "AI Agent Bridge: Toggle Claude Subscription Source"
			},
			{
				"command": "llamacpp.claudeSignIn",
				"title": "AI Agent Bridge: Sign In to Claude Subscription"
			},
			{
				"command": "llamacpp.claudeSignOut",
				"title": "AI Agent Bridge: Sign Out of Claude Subscription"
			},
			{
				"command": "llamacpp.claudeShowStatus",
				"title": "AI Agent Bridge: Show Claude Subscription Status"
			},
			{
				"command": "llamacpp.claudeContinueLatestInNewChat",
				"title": "AI Agent Bridge: Continue Latest Claude Session in New Chat"
			},
			{
				"command": "llamacpp.setClaudeContextLength",
				"title": "AI Agent Bridge: Set Claude Working Context"
			},
			{
				"command": "llamacpp.openContextControl",
				"title": "AI Agent Bridge: Open Provider Context Control"
			},
			{
				"command": "llamacpp.setApiKey",
				"title": "AI Agent Bridge: Set Primary API Key"
			},
			{
				"command": "llamacpp.openSettings",
				"title": "AI Agent Bridge: Open Settings",
				"icon": "$(settings-gear)"
			},
			{
				"command": "llamacpp.openMemory",
				"title": "AI Agent Bridge: Open Shared Memory"
			},
			{
				"command": "llamacpp.openApiProviders",
				"title": "AI Agent Bridge: Providers Manager",
				"icon": "$(server-process)"
			},
			{
				"command": "llamacpp.clearMemory",
				"title": "AI Agent Bridge: Clear Shared Memory"
			},
			{
				"command": "llamacpp.setThinkingMode",
				"title": "AI Agent Bridge: Set Thinking Mode"
			},
			{
				"command": "llamacpp.setCompactionTargetRatio",
				"title": "AI Agent Bridge: Set Compaction Target"
			},
			{
				"command": "llamacpp.setReasoningBudget",
				"title": "AI Agent Bridge: Set Local Reasoning Cap"
			},
			{
				"command": "llamacpp.setToolResultMode",
				"title": "AI Agent Bridge: Set Tool Result Mode"
			},
			{
				"command": "llamacpp.setToolCallingMode",
				"title": "AI Agent Bridge: Set Tool Calling Mode"
			},
			{
				"command": "llamacpp.setKnowledgeMode",
				"title": "AI Agent Bridge: Set Knowledge Verification"
			},
			{
				"command": "llamacpp.refreshModels",
				"title": "AI Agent Bridge: Refresh Models",
				"icon": "$(refresh)"
			},
			{
				"command": "llamacpp.openCopilotModelPicker",
				"title": "AI Agent Bridge: Choose Chat Model",
				"icon": "$(comment-discussion)"
			},
			{
				"command": "llamacpp.applyCopilotPatch",
				"title": "AI Agent Bridge: Apply Copilot Chat Patch"
			},
			{
				"command": "llamacpp.copilotPatchStatus",
				"title": "AI Agent Bridge: Show Copilot Chat Patch Status"
			},
			{
				"command": "llamacpp.restoreCopilotPatch",
				"title": "AI Agent Bridge: Restore Original Copilot Chat"
			},
			{
				"command": "llamacpp.openLogsFolder",
				"title": "AI Agent Bridge: Open Logs Folder"
			},
			{
				"command": "llamacpp.runHealthCheck",
				"title": "AI Agent Bridge: Run Provider Health Check"
			},
			{
				"command": "llamacpp.openSessionReport",
				"title": "AI Agent Bridge: Open Session Quality Report"
			},
			{
				"command": "llamacpp.resetSessionReport",
				"title": "AI Agent Bridge: Reset Session Metrics"
			},
			{
				"command": "llamacpp.clearTokenUsageHistory",
				"title": "AI Agent Bridge: Clear Token Usage History"
			},
			{
				"command": "llamacpp.startBaselineUsageExperiment",
				"title": "AI Agent Bridge: Start Baseline Usage Experiment"
			},
			{
				"command": "llamacpp.startDelegatedUsageExperiment",
				"title": "AI Agent Bridge: Start Delegated Usage Experiment"
			},
			{
				"command": "llamacpp.stopUsageExperiment",
				"title": "AI Agent Bridge: Stop and Export Usage Experiment"
			},
			{
				"command": "llamacpp.exportUsageExperiment",
				"title": "AI Agent Bridge: Export Usage Experiment Report"
			},
			{
				"command": "llamacpp.clearUsageExperiments",
				"title": "AI Agent Bridge: Clear Usage Experiments"
			},
			{
				"command": "llamacpp.openLatestLog",
				"title": "AI Agent Bridge: Open Latest Log"
			},
			{
				"command": "llamacpp.copyLatestLogPath",
				"title": "AI Agent Bridge: Copy Latest Log Path"
			},
			{
				"command": "llamacpp.toggleFileLogging",
				"title": "AI Agent Bridge: Toggle File Logging"
			},
			{
				"command": "llamacpp.toggleStreamChunkLogging",
				"title": "AI Agent Bridge: Toggle Stream Chunk Logging"
			},
			{
				"command": "llamacpp.togglePerformanceStatusBar",
				"title": "AI Agent Bridge: Toggle Performance Status Bar"
			},
			{
				"command": "llamacpp.toggleContextUsageStatusBar",
				"title": "AI Agent Bridge: Toggle Context Usage Status Bar"
			},
			{
				"command": "llamacpp.toggleClaudeCacheKeepAlive",
				"title": "AI Agent Bridge: Toggle Claude Cache Keep-Alive",
				"category": "AI Agent Bridge"
			}
		],
		"menus": {
			"view/title": [
				{
					"command": "llamacpp.openCopilotModelPicker",
					"when": "view == llamacpp-quick-actions",
					"group": "navigation@1"
				},
				{
					"command": "llamacpp.refreshModels",
					"when": "view == llamacpp-quick-actions",
					"group": "navigation@2"
				},
				{
					"command": "llamacpp.openSettings",
					"when": "view == llamacpp-quick-actions",
					"group": "navigation@3"
				}
			]
		}
	},
	"main": "./out/extension.js",
	"scripts": {
		"vscode:prepublish": "npm run clean && npm run compile",
		"clean": "node -e \"require('node:fs').rmSync('out', { recursive: true, force: true })\"",
		"patch:copilot": "npm run compile --silent && node ./scripts/patch-copilot-chat.mjs apply",
		"patch:copilot:status": "npm run compile --silent && node ./scripts/patch-copilot-chat.mjs status",
		"patch:copilot:restore": "npm run compile --silent && node ./scripts/patch-copilot-chat.mjs restore",
		"update-vscode-api": "dts main && node ./scripts/move-vscode-api.mjs",
		"compile": "tsc -p ./",
		"lint": "eslint",
		"check": "npm run lint && npm test",
		"test:claude-native": "node ./scripts/claude-native-sdk-smoke.mjs",
		"test:codex-persistence": "node ./scripts/codex-thread-persistence-smoke.mjs",
		"package": "vsce package",
		"format": "prettier --write .",
		"watch": "tsc -watch -p ./",
		"test": "npm run compile && vscode-test"
	},
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@stylistic/eslint-plugin": "^5.10.0",
		"@types/mocha": "^10.0.6",
		"@types/node": "^26",
		"@types/vscode": "^1.125.0",
		"@vscode/dts": "^0.4.1",
		"@vscode/test-cli": "^0.0.15",
		"@vscode/test-electron": "^3.1.0",
		"@vscode/vsce": "3.9.2",
		"eslint": "^10.8.1",
		"prettier": "^3.9.6",
		"typescript": "^5.9.3",
		"typescript-eslint": "^8.67.0"
	},
	"overrides": {
		"diff": "8.0.3",
		"serialize-javascript": "7.0.7"
	},
	"packageManager": "npm@10.9.8",
	"dependencies": {
		"@anthropic-ai/claude-agent-sdk": "0.3.228",
		"zod": "^4.4.3"
	}
}
